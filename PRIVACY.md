# 隐私政策（Privacy Policy）

**生效日期：2026 年 9 月 26 日** ｜ 适用于 LxAI 客户端软件（Android / Windows）与 LxAI 中继服务。

> 本政策说明：**中继到底保存了什么、保存在哪、保存多久、怎么删**。自建中继的实例由部署者按本政策自行运营；官方托管实例由作者运营。

---

## 一、我们保存什么

| 类别 | 具体内容 | 说明 |
| --- | --- | --- |
| 账号 | 用户名、**密码哈希（bcrypt，不可逆）** | 不保存明文密码 |
| 设置 | 客户端设置项：模型 API 端点 URL、**你填写的 API Key**、Agent 配对 Token、界面与偏好设置 | 见下方「加密存储」：Key / Token 以 **AES-256-GCM 加密**后落盘 |
| 消息 | 你与 AI / Agent 的对话内容（含 Agent 执行记录、思考内容）、消息时间与会话归属 | 落盘于 `messages_data/`，默认保留 **180 天** |
| 媒体 | 你主动发送的图片、文件、语音（如使用） | 落盘于 `messages_media/`，默认保留 **90 天** |
| 会话状态 | 单点登录心跳：设备类型、客户端会话 ID、在线状态 | 用于 1 手机 + 1 电脑的互斥登录 |
| 桥接凭证 | 电脑端 Bridge 的设备凭证（用于 Token 换发后自动重连） | `device_tokens.json` |
| 待办 | 挂起中的选择框 / 授权请求（含问题内容与工具名） | `pending-decisions.json`，**24 小时后自动清理** |
| 运行日志 | 连接、同步、错误等运行记录（不含密码与密钥） | `server-run.log` |

**加密存储（重要）**

- 设置中的 API Key、ASR Key、Agent 配对 Token 使用 **AES-256-GCM** 加密后写入磁盘（格式 `enc:v1:…`）。
- 加密主密钥保存在 **数据目录之外**（`<部署目录>/.secrets/master.key`，可用环境变量 `SETTINGS_ENC_KEY` 覆盖）——
  目的是：**只拿到数据文件的副本（备份外流、误发、被拖库）无法解出明文密钥**。
- 请部署者**备份主密钥**：丢失后已加密的密钥无法恢复（用户在 App 里重新填写即可，不影响其他数据）。
- 部署者也可以选择 **完全不落盘密钥**：设置环境变量 `STORE_API_KEYS=0`，服务端保存设置时会清空这些字段
  （代价：换设备后需要重新填写 Key；日常调用模型时用的是客户端随请求带来的 Key，因此不影响功能）。


**我们不收集**：手机号、邮箱、通讯录、短信、精确位置、相册扫描、设备上与本软件无关的文件。本软件不读取你的电脑文件，除非你在 Agent 会话里明确要求它这么做（该行为由本地 Agent 执行，属于你的主动指令）。

## 二、数据保存在哪里

1. **中继服务端**：你使用的中继实例所在服务器（自建 = 你自己的服务器；官方托管 = 作者的服务器），位于上述 `messages_data/`、`messages_media/` 与日志文件中。
2. **你的设备本地**：手机 / 电脑客户端在本机保存会话与消息缓存（用于离线查看与增量同步）。
3. **电脑端 Bridge**：仅在本机保存一个可选的运行日志文件（可通过启动参数关闭或限制大小）。

## 三、数据会被发给谁

1. **你配置的模型服务商**：你发送的消息会经中继转发到**你自己配置的**模型 API 端点（例如你自己的 API Key 对应的服务商）。该部分数据的处理受**对应服务商的隐私政策**约束，与作者无关。
2. **你的电脑端 Agent**：任务与消息会转发给你自己的电脑（Bridge → 本地 Agent）。
3. **我们不向任何第三方出售、出租或共享你的数据**，法律法规另有强制要求的除外（届时会在法律允许范围内告知）。

## 四、保存多久

| 数据 | 默认保留期 | 可调整方式 |
| --- | --- | --- |
| 聊天消息 | **180 天** | 环境变量 `MESSAGE_RETENTION_DAYS`（设 `0` = 永久保存） |
| 图片 / 文件 / 语音 | **90 天** | 环境变量 `MEDIA_RETENTION_DAYS`（设 `0` = 永久保存） |
| 挂起中的选择框 / 授权请求 | **24 小时**（自动清理） | 固定 |
| 运行日志 | 由部署者自行轮转 | — |

- 清理任务在服务启动 60 秒后执行一次，之后每 6 小时巡检一次，只删除**超过保留期**的数据（时间戳解析不出来的记录一律保留，避免误删）。
- 你可以随时手动删除消息或申请删除账号数据，不必等保留期到期。
- 服务端清理**不影响**你设备本地已保存的记录；反之，本地删除会同步删除服务端对应记录。


## 五、安全措施与风险提示

**已采取的措施**

- 密码使用 bcrypt 哈希存储，不可逆；
- **API Key / Agent Token 以 AES-256-GCM 加密落盘**，主密钥保存在数据目录之外（见第一节）；
- 账号级隔离：Agent Token 与账号绑定，越权访问会被拒绝；
- 单点登录互斥，账号异常登录会互相顶下线；
- 本地服务默认只监听回环地址（`127.0.0.1`），不对外暴露；
- 默认保留期（消息 180 天 / 媒体 90 天）定期自动清理，减少长期沉淀的敏感数据。

**你需要知道的风险**

- 加密解决的是"数据文件被单独拿走"这类风险；**如果服务器被完整攻破（含主密钥文件）**，仍可能被解密。对数据特别敏感时，建议**自建中继**并设置 `STORE_API_KEYS=0` 让密钥完全不落盘。
- 请为客户端配置**权限最小、可随时吊销**的 API Key，不要使用主账号密钥。
- 请勿将本地 3080 端口直接暴露到公网；公网部署请自行配置 HTTPS 反向代理。
- 任何联网服务都无法保证绝对安全。

## 六、你的权利

1. **删除消息**：在 App 内删除消息会同步删除服务端对应记录。
2. **删除账号数据**：可通过仓库 issue 或你部署实例的管理员联系方式提出申请，我们将在核实身份后删除账号、设置、消息与媒体文件。
3. **导出**：你的消息与设置保存在服务端 JSON 文件中，自建实例可直接导出。
4. **撤回同意**：停止使用并申请删除数据即可；无需说明理由。

## 七、未成年人

本软件面向具备完全民事行为能力的用户。若你为未成年人，请在监护人陪同下阅读本政策并在其同意后使用。

## 八、政策变更

本政策更新后在本仓库公布即生效；涉及重大变更时会在 App 内或仓库公告中提示。继续使用即视为接受更新后的政策。

## 九、联系方式

- 仓库：https://github.com/lx00924-lx/flutter-app
- 通过仓库 issue 提交隐私相关问题或删除申请。

---

*本文件为通用隐私政策模板，不构成法律意见；如用于正式商业运营，建议经执业律师审阅。*
